# I18n.t('product_price', price: @product.price)
#
# en:
#  product_price: "$%{price}"

en:
  test: "This is a test" # Do not delete (for tests)
  test_base:
    _integer: "one"
    _preserve: "two"
    _display_as: "three"
    _default: "default"
    _multiline: "wild"
